//
//  babbling.swift
//  Programmers
//
//  Created by 김보영 on 2023/02/04.
//

import Foundation

let cousinVocab = ["aya", "ye", "woo", "ma"]

/*
 
 일단 가장 단순한 옹알이는
 "aya", "ye", "woo", "ma"
 
 1 ≤ babbling의 길이 ≤ 100
 1 ≤ babbling[i]의 길이 ≤ 15
 babbling의 각 문자열에서 "aya", "ye", "woo", "ma"는 각각 최대 한 번씩만 등장합니다.
 즉, 각 문자열의 가능한 모든 부분 문자열 중에서 "aya", "ye", "woo", "ma"가 한 번씩만 등장합니다.
 문자열은 알파벳 소문자로만 이루어져 있습니다.
 
 유의사항
 
 네 가지를 붙여 만들 수 있는 발음 이외에는 어떤 발음도 할 수 없는 것으로 규정합니다. 예를 들어 "woowo"는 "woo"는 발음할 수 있지만 "wo"를 발음할 수 없기 때문에 할 수 없는 발음입니다.
 
 */

func IsItBabblable() -> Bool {
    
 return
    
}

/*
 반복문을 이용해서 babbling 배열 길이만큼 반복한다 {
 
    cousinVocab.length 만큼 반복해서 {
 
        if ( 만약 babbling[i]에 cousinVocab[j]이 맨 앞에 붙어 있다면 ) {

 
 }
 */
